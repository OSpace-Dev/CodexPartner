# 开发说明

## 本地调试

1. 在 VS Code 中打开本仓库根目录。
2. 按 `F5`，选择 `Run Codex Partner`，启动 Extension Development Host。
3. 在 Extension Development Host 中验证扩展行为。

## 自动化检查

运行 `npm run check` 执行 JavaScript 语法检查和引用格式单元测试。
