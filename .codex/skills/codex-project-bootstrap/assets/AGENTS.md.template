# AGENTS.md

## 定位

本文件用于定义 Agent 在当前仓库中的稳定、执行期协作规则。

保持项目事实、命令、目录归属和当前交付约束的准确性。长说明、决策记录和持续变化的背景应放在仓库指定的文档位置。

## 指令发现

- 在规划或编辑前先阅读本文件。
- 定位并遵循待修改目录中更具体的 `AGENTS.md`。
- 将仓库本地指令视为本仓库的权威约束。
- 在[指定决策位置]记录需要长期保留的项目决策；不得只依赖对话历史。

## 生命周期路由

- 用户可以直接用自然语言描述软件任务，不要求显式输入 Skill 名称。
- 请求只有一个想法、阶段不明确、询问下一步、继续既有工作或跨越多个阶段时，优先使用 `$software-development-lifecycle` 判断阶段。
- 请求已经明确属于需求、界面、架构、规划、实现、评审、测试、部署或运维阶段时，可以直接使用对应专项 Skill。
- 不要一次加载所有 Skill；只加载当前阶段和确有依赖的少量 Skill。

## 交付流程

1. 确认请求目标、范围、非目标和验收条件。
2. 修改文件前，检查相关代码、测试、配置和已有文档。
3. 识别必须确认的决策：产品行为、接口、数据、安全、成本、架构或兼容性。
4. 进行小而内聚的改动，避免无关清理。
5. 对受影响范围执行所需验证。
6. 更新任务记录和受影响文档。
7. 说明改动、已执行的验证、假设和剩余风险。

## 需求与决策

- 区分已确认事实、推断、假设和待决策事项。
- 未决选择可能影响行为、契约、安全、数据、成本或架构时，不得猜测。
- 依赖可回退、低影响的假设前，先声明该假设。
- 在[指定决策位置]记录长期决策，包括背景、备选方案、结论和后果。
- 在[指定知识位置]记录重要故障或修复，包括现象、证据、根因、修复动作和后续排查方法。

## 变更安全

- 优先进行小步、可审查、可回退的改动。
- 除非已确认任务要求变更，否则保持现有约定。
- 修复缺陷前，在可行时建立可复现证据或回归测试。
- 未经必要确认，不得变更公开契约、持久化数据、安全控制、依赖或模块边界。
- 不得提交密钥、凭据、机器专用路径或生成的临时产物。

## 代码质量

- 实现和 Review 时使用 `$code-quality`，并遵循项目已有格式化、Lint、类型和静态分析配置。
- 质量改进服务于当前任务，不静默扩大为无关重构。
- 坏味道必须说明行为或维护影响；不把个人格式偏好当成缺陷。
- 需要新增或修改项目编码规范时，记录到[指定规范位置]并说明适用范围。

## 版本控制

- Git 操作使用 `$git-workflow`，提交前检查工作树、暂存差异、验证结果和隐私信息。
- 只暂存当前任务内容，不覆盖、丢弃或混入来源不明的现有改动。
- 提交、推送、合并、标签、远端修改和历史重写遵循用户授权与仓库规则。
- 托管平台与远端用途：[GitHub / GitLab / Gitee / 自建服务及用途]
- 分支和提交约定：[填写已确认规则]

## 路径与环境隐私

- 可提交的文档、Markdown 链接、代码注释、示例命令、测试说明和配置必须使用仓库相对路径。
- 不得写入盘符、系统用户目录、真实用户名、工作区上级目录、IDE 缓存目录或本地文件与 IDE 专用 URI。
- 运行时确实需要绝对路径时，通过环境变量、命令行参数或 Git 忽略的本地配置传入。
- 提交前检查变更，确认不存在本机绝对路径或可识别开发环境的信息。

## 验证

- 执行仓库要求的命令：[测试命令]、[类型检查命令]、[Lint 命令]、[构建命令]。
- 行为发生变化时，新增或更新聚焦的测试。
- 自动化验证不足时，执行[人工验证要求]。
- 说明未执行的验证及其原因。

## 仓库地图

- 应用代码：[路径和归属]
- 测试：[路径和约定]
- 文档：[路径和导航文件]
- 决策与规范：[路径]
- 临时或生成产物：[路径和保留规则]
- 受限区域：[路径和原因]

## 沟通

- 进行较大改动前，说明意图、范围和预计受影响区域。
- 遇到阻塞时，说明已检查的证据、准确阻塞点和所需的最小决策项。
- 完成后，说明改了什么、如何验证和剩余风险。

## 仓库专用规则

<!-- 添加无法从代码推断的简洁、稳定规则；未使用时删除本节。 -->

- [包管理器和必需命令]
- [支持的运行时版本]
- [分支、提交或 Pull Request 约定]
- [部署、环境或合规限制]
