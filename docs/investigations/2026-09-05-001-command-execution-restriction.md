# 命令执行限制排查记录

编号：`INV-20260905-001`
日期：`2026-09-05`

## 现象

新会话中，普通沙箱执行无害命令时出现：`helper_unknown_error: setup refresh had errors`，进程无法创建。此前一次授权审查还出现过 HTTP 502；本轮个别授权请求出现自动审批审查超时。

## 对照结果

- 普通沙箱：`Get-Location; node --version` 仍无法创建进程。
- 已授权执行：同一目录执行成功，Node.js 版本为 `22.17.1`。
- 已授权执行：`npm.cmd run check` 成功，17 个测试全部通过。
- 已授权执行：本机已有 `vsce` 可用，并成功生成 `codex-partner-0.5.0.vsix`。
- 仓库 `AGENTS.md` 的现有改动只删除了 Windows 工具映射和命令限制条款，没有修改项目脚本或 Node 配置。

## 结论

直接原因可以确认是 Codex 命令运行器的沙箱 helper 初始化或刷新异常；授权执行路径本身可用。根据当前证据，这不是插件代码、`package.json`、Node.js 或 `AGENTS.md` 导致的失败。

“helper 初始化异常”和“授权审查超时/502”属于执行基础设施层问题。本轮没有可从仓库内修复的配置项，也没有证据表明项目改动会持久触发该问题。当前可行的临时处理是由用户明确授权后使用授权执行路径，并保留原始错误用于后续桌面端或运行器日志排查。

## 未覆盖范围

当前会话无法访问 Codex 桌面端沙箱 helper 和审批服务的内部日志，因此不能进一步定位是本机环境、会话初始化状态还是服务端短暂故障。若后续普通沙箱仍持续失败，应在重启 Codex 会话后用同一条无害命令复测，并将完整错误交给运行器维护方。