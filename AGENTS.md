# AGENTS.md

## 定位

本文件定义 Agent 在当前仓库中的稳定、执行期协作规则。项目事实和长期记录以仓库文档为准；创建文档目录后，统一从 `docs/README.md` 导航，不用对话历史代替仓库记录。

## Windows 开发环境（强制规范）

当前项目的开发环境是 Windows，仓库内容包含中文目录、中文文件名和中文文本。所有 Agent 必须遵守以下工具映射，不得因为习惯直接套用其他操作系统的命令。

### 工具映射表

| 操作 | 首选工具 | 禁止使用 |
| --- | --- | --- |
| 读文件 | `Read` | `cat`、`head`、`tail`、`type`、`Get-Content` |
| 搜文件 | `Glob` | `find`、`ls`、`dir`、`Get-ChildItem` |
| 搜内容 | `Grep` | `grep`、`rg`、`Select-String` |
| 编辑文件 | `Edit` | `sed`、`awk`、`perl`、PowerShell 文本替换脚本 |
| 创建文件 | `Write` | `echo >`、`printf >`、重定向写文件、PowerShell 写文件脚本 |
| 执行系统命令 | `Bash` | PowerShell、`cmd.exe`、批处理命令 |

表中的工具名表示优先使用的文件和命令能力。如果当前运行器没有单独暴露这些工具，必须使用平台提供的等价文件工具；不得因此改用 PowerShell、`cmd.exe` 或被列为禁止的搜索命令。当前 Codex 运行器只有通用命令入口时，系统命令应明确通过 Git Bash 执行，文件编辑使用补丁编辑能力，不要把 PowerShell 命令写进项目脚本或部署文档。

### 中文路径与编码

- 所有文本文件统一保持 UTF-8；读取、编辑、生成时不得改变中文内容、换行风格或文件编码。
- 处理中文路径、空格、括号或以连字符开头的路径时必须加引号；Git 路径参数前使用 `--`，避免路径被解析成选项。
- 不得通过临时编码转换、GBK/ANSI 默认编码或命令行重定向改写文件。
- 搜索和批量操作必须先限制目标目录，再执行具体操作；不要对工作区根目录使用无边界递归命令。
- 文档中的项目路径使用仓库相对路径；本机绝对路径只允许出现在工具调用参数中，不得写入可提交文件。

### Bash 与目标环境边界

- 本地 Windows 命令使用 Bash 语法；项目要求的 `pnpm`、Git 和检查命令统一从 Bash 执行。
- 面向 Linux 服务器的命令只能在服务器 Bash 或本机 Git Bash 中验证；不得把 PowerShell 变量、管道、引号或路径语法混入 Bash 命令。
- `docker compose`、`corepack pnpm` 和 `git` 命令执行前，先确认当前工作目录；跨目录操作使用明确的工作目录参数。
- 任何删除、移动、覆盖或批量修改前，先解析并确认目标路径位于预期目录；禁止使用无边界的递归删除或覆盖命令。
- 命令失败时保留原始错误并修正命令，不得通过改写编码、关闭错误处理或静默忽略错误来“绕过”失败。

## 指令发现

- 在规划或编辑前先阅读本文件。
- 修改子目录前，查找并遵循作用域更具体的 `AGENTS.md`；当前仓库没有嵌套指令。
- `.codex/skills/` 保存仓库本地的软件交付生命周期 Skills 及其参考资料。使用 Skill 时必须完整读取对应 `SKILL.md`，并按其中的引用规则加载必要资料。
- 不修改 `.codex/skills/`，除非任务明确要求维护这些 Skills。

## 生命周期路由

- 用户可以直接用自然语言描述软件任务，不要求显式输入 Skill 名称。
- 只有想法、阶段不明确、询问下一步、继续既有工作或跨越多个阶段时，使用 `$software-development-lifecycle` 判断阶段。
- 阶段明确时，直接使用对应的需求、前端设计、架构、规划、实现、评审、测试、部署或运维 Skill。
- 实现与代码评审按需使用 `$code-quality`；任何 Git 操作使用 `$git-workflow`。
- 涉及界面或交互的设计、实现、修复与评审时，必须加载全局 `ui-ux-pro-max` Skill；Vue 任务同时遵循 `vue-best-practices`。
- 一次只加载当前阶段和必要依赖的少量 Skills，不预先加载整个生命周期。

## 交付规则

1. 先确认目标、范围、非目标、验收条件和影响交付的未决事项。
2. 修改前检查相关代码、测试、配置和文档，并保护来源不明的现有改动。
3. 进行小而内聚、可审查、可回退的改动，不夹带无关清理。
4. 对受影响范围执行验证；行为变化应配套聚焦测试。
5. 更新对应的 system of record，并在完成时说明改动、验证、假设和剩余风险。

产品行为、公开契约、数据、安全、成本、架构、兼容性或依赖发生实质变化时，不得自行猜测关键决策。待确认事项记录在 `docs/questions/`，确认后的结论移入对应需求或决策文档，并从原问题链接过去。

## 代码质量

- 遵循项目已有格式化、Lint、类型检查和静态分析配置；不存在配置时，不臆造工具或命令。
- 优先保证命名清晰、职责内聚、控制流直接、错误可诊断、依赖显式且行为可测试。
- 质量改进服务于当前任务，不静默扩大为无关重构；评审意见必须说明行为或维护影响。
- 稳定、可复用的项目编码规范归档到 `docs/standards/`，在确有规范需要时再创建该目录。

## 版本控制

- 当前仓库根目录尚未初始化为 Git 仓库，也未发现独立的应用子仓库。创建或接入 Git 前，先确认仓库边界、远端、分支和提交约定。
- Git 可用后，操作前后检查工作树和差异，只暂存当前任务内容，不覆盖、丢弃或混入来源不明的改动。
- 提交、推送、合并、标签、远端修改和历史重写必须符合用户授权与仓库约定。
- 不提交密钥、凭据、本机绝对路径、环境身份信息或临时生成物。

## 验证

- 当前原型使用 Node.js 和 VS Code Extension API；包清单为 `package.json`。
- JavaScript 语法检查和 App Server 客户端测试：`npm run check`。
- 只读 App Server 协议探针：`npm run probe`；Codex 不在 `PATH` 时传入 `--codex <path>`。
- 当前环境的 npm 启动器可能不可用时，使用已确认可用的 Node.js 运行时直接执行清单中的等价 `node --check` 和 `node --test` 命令。
- 新增技术栈或工具链时，在其清单和配置中建立真实命令，并同步更新本文件。
- 自动化验证不足时，记录可复现的人工步骤、预期结果和未覆盖风险。
- 完成任务时明确列出已执行和未执行的验证及原因。

## 仓库地图与文档所有权

- `.codex/skills/`：当前仓库内置的 Skills、Agent 元数据和参考资料。
- `AGENTS.md`：当前仓库的稳定、执行期协作规则。
- `docs/`：项目文档目录，统一从 `docs/README.md` 导航。
- `src/`：扩展宿主侧的 App Server 客户端。
- `media/`：侧边栏 Webview 的脚本和样式。
- `test/`：Node.js 内置测试，覆盖 App Server 客户端协议边界。
- `scripts/`：只读验证和探针脚本。
- `docs/questions/`、`docs/requirements/`、`docs/architecture/`、`docs/ui/`、`docs/planning/`、`docs/reviews/`、`docs/testing/`、`docs/deployments/`、`docs/operations/`、`docs/decisions/`、`docs/standards/` 和 `docs/investigations/`：仅在首次产生相应文档时创建，不预先创建空目录。

一个主题只保留一个 system of record；其他位置只做导航或链接。文档默认使用 `YYYY-MM-DD-NNN-topic.md` 命名，同一类别按日期独立编号，编号一经分配不因移动或改名而改变。

## 路径与环境隐私

- 可提交的文档、Markdown 链接、代码注释、示例命令、测试说明和配置使用仓库相对路径。
- 不写入盘符、系统用户目录、真实用户名、工作区上级目录、本机地址、本地文件 URI 或 IDE 专用 URI。
- 运行时确需绝对路径时，通过环境变量、命令行参数或 Git 忽略的本地配置传入。
- 提交或交付前扫描变更，确认不存在密钥、本机路径和环境身份信息。

## 沟通

- 较大改动前说明意图、范围和预计受影响区域。
- 阻塞时说明已检查的证据、准确阻塞点和所需的最小决策。
- 不确定内容必须标记为推断、假设或待确认，不得写成项目事实。
